#include <lox_parser.h>


auto main(int argc, char** argv) -> int{
  if (argc < 0) {
    // error, not allowed operation 

  } else if (argc == 1) {
    // run file :argv[0]

  } else {
    // run_prompt()
  }

}
